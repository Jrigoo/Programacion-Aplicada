/* 
Access memory address

cuando declaramos una variable esta variable se guarda en la memoria de nuestra computadora ej. int age = 30. Este valor se guarda en un valor especifico de la memoria RAM

Para referirnos a una variable la llamamos. Sin embargo, C no hace eso, sino accede directamente a la memoria

Para saber esto podemos imprimirlos como.
    printf("%p",&age)


Pointers
- Tipo de data que podemos usar en nuestros programas
- Es un memory address, physical address en nuestra pc donde guardamos un valor
- Sigue siendo como un int o double, pero en vez de ser decimal es un address de la memoria


Para nombrar un pointer empiezas con una p y luego el nombre real
ej. pAge

 */
#include<stdio.h>

void main(){

}